# Tasks

- [ ] Route normal left click directly to Hidden Items Panel when the feature is enabled.
- [ ] Add distinct primary/right-click interaction callbacks for hidden panel rows.
- [ ] Invoke hidden AX menu extras directly without expanding the menu bar first.
- [ ] Cache the Ops Notch context menu and refresh it when settings/state change.
- [ ] Update Chinese and English interaction hints.
- [ ] Run `swift test`, `swift build`, and `python3 scripts/static_checks.py` in CI.
- [ ] Manually verify macOS left-click panel entry, panel item left/right click, notch overflow behavior, and context-menu responsiveness.
