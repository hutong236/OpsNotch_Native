# Tasks

- [x] Remove ordinary pointer hover callbacks from `SensorManager`.
- [x] Remove hover activation geometry, delay, timers, and movement intent logic from `SensorView`.
- [x] Keep native drag/drop callbacks and Sensor bounds unchanged.
- [x] Remove `HoverIntentPolicy` and related unit tests.
- [x] Document pointer-inert Sensor behavior.
- [ ] Verify `swift test`.
- [ ] Verify `swift build`.
- [ ] Verify `python3 scripts/static_checks.py`.
- [ ] Perform real-Mac smoke test for drag/drop after merge.
