# Tasks

- [x] Remove Sensor indicator visibility state from `SensorManager`.
- [x] Remove indicator geometry and drawing code from `SensorView`.
- [x] Preserve Sensor panel bounds and native drag/drop registrations.
- [x] Keep `setShelfVisible` as a compatibility no-op.
- [x] Run `swift test`.
- [x] Run `swift build`.
- [x] Run `python3 scripts/static_checks.py`.
- [ ] Manual smoke test on macOS: no idle pill/dot, drag/drop still works.
