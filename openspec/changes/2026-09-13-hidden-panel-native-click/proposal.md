# 隐藏图标面板原生点击修复

关联 Issue #100。

## 问题

面板目前只调用 AXPress / AXShowMenu。AX 返回成功不代表第三方菜单已显示，且右键回退到 AXPress 会改变语义。用户仍需先显示全部图标才能操作。

## 目标与范围

在面板单次左键或右键后，直接向原图标投递相应的原生鼠标事件。保持隐藏区域和排列不变，不要求用户再点击真实菜单栏。不涉及暂存区、桌面切换或持久化格式。
