# 修复真实隐藏图标的窗口定位

PR #101 合并后，实机在持续隐藏区右键 Clash Verge、OneDrive、TCP Latency 时出现“未能连接到该图标”。旧定位器要求 AX 元素矩形和 layer=25 的窗口矩形完全一致；此前离屏窗口探针没有覆盖带边距的 AX 子控件。

目标：优先通过 AX 窗口编号定位，使用图标相对于其所属窗口的坐标；保留一次原生左右键点击和现有隐藏布局。范围仅为定位、失败诊断及回归验证。
