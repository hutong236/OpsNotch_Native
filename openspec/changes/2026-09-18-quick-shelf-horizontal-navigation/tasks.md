# Tasks

- [x] 定义 ← 智能最近首条 / → Finder 快捷目录首条的行为边界。
- [x] Core：新增可测试的 `QuickShelfKeyboardNavigation`。
- [x] Core Tests：覆盖左右目标与目标区域为空。
- [x] AppModel：把当前可见 Finder / Smart Recent 映射到 Core 导航逻辑。
- [x] AppKit：在 Shelf 本地键盘监控中接入 ← / →。
- [ ] `swift test` 通过。
- [ ] `swift build` 通过。
- [ ] `python3 scripts/static_checks.py` 通过。
- [ ] GitHub Actions 通过。
